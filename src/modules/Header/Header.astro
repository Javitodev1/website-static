---
import Button from '@/components/Button.astro'
import Dropdown from '@/components/Dropdown/Dropdown.astro'

import Logo from '@/assets/logo.svg'
import { LINK } from '@/consts/link'

import Navbar from './components/Navbar.astro'
import NavBrand from './components/NavBrand.astro'
import Hamburger from './components/Hamburger.astro'
import NavUl from './components/NavUl.astro'
import NavLi from './components/NavLi.astro'

import WebdevIcon from '@/assets/icons/navbar-desarrollo-web.svg'
import DesignIcon from '@/assets/icons/navbar-diseño-grafico.svg'
import AdsIcon from '@/assets/icons/navbar-publicidad.svg'
import type { CMSImage } from '@/types/content'

const webdevIcon: CMSImage = {
  url: WebdevIcon.src,
  width: WebdevIcon.width,
  height: WebdevIcon.height,
  alternativeText: 'Suads Agency | Servicio de desarrollo web',
}

const designIcon: CMSImage = {
  url: DesignIcon.src,
  width: DesignIcon.width,
  height: DesignIcon.height,
  alternativeText: 'Suads Agency | Servicio de Diseño Grafico',
}

const adsIcon: CMSImage = {
  url: AdsIcon.src,
  width: AdsIcon.width,
  height: AdsIcon.height,
  alternativeText: 'Suads Agency | Servicio de Publicidad Digital',
}
---

<Navbar id='header'>
  <NavBrand>
    <Logo />
  </NavBrand>

  <Hamburger id='hamburger' />

  <NavUl id='navbar'>
    <NavLi
      href={LINK.root}
      text='Home'
    />

    <Dropdown
      name='services'
      menuClass='bg-primary-1000/95 inset-shadow-card-sm inset-shadow-blue-700/5 border-l-2 border-b-2 border-blue-700/15'>
      <Fragment slot='header'>Servicios</Fragment>
      <NavLi
        href={LINK.webdev}
        text='Desarrollo Web'
        icon={webdevIcon}
      />
      <NavLi
        href={LINK.ads}
        text='Publicidad Digital'
        icon={adsIcon}
      />
      <NavLi
        href={LINK.design}
        text='Diseño Gráfico'
        icon={designIcon}
      />
    </Dropdown>

    <Button
      href={LINK.contact}
      cta='Contacto'
    />
  </NavUl>
</Navbar>

<script src='./lib/navbar.ts'></script>
